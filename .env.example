# Database Configuration
# For Vercel Postgres (Production)
POSTGRES_PRISMA_URL="postgresql://user:password@host:5432/database?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host:5432/database"

# For Local Development with PostgreSQL (Optional)
# If you want to test with PostgreSQL locally, uncomment and configure:
# DATABASE_URL="postgresql://user:password@localhost:5432/restaurant_qr_dev"

# Note: Vercel will automatically populate POSTGRES_PRISMA_URL and POSTGRES_URL_NON_POOLING
# when you add Vercel Postgres storage to your project.
